#!/usr/bin/env node
/**
 * No-op script - games infrastructure removed per overhaul plan
 * The games.json generation is no longer needed as we're focusing
 * on APK/EXE compilation rather than HTML5 games.
 */

// Nothing to do - exit successfully
process.exit(0);
